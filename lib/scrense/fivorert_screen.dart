import 'package:flutter/material.dart';

class favoritScreen extends StatelessWidget {
  const favoritScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("صفحات الرحلات المفضلة"),
    );
  }
}
